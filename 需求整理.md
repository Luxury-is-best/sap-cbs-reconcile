# SAP 与 CBS 对账需求整理

## 一、目标
识别 **CBS 明细中未在 SAP 明细中存在** 的记录，并标注匹配状态、生成对账报告。

## 二、数据源（4 个表格）

| 表格     | 说明       | 关键字段（对账用） |
|----------|------------|--------------------|
| CBS 明细 | 交易明细   | 交易日期、交易流水号、账号、借(支出)/贷(收入)、银行流水号、对账码 |
| CBS 余额 | 账户余额   | 账号、账户名称、账户余额、币种、开户行 |
| SAP 明细 | 交易明细   | 凭证编号、过账日期、公司代码货币价值、借/贷标识、总帐科目等 |
| SAP 余额 | 账户余额   | 科目号、余额、货币、期间/年度、利润中心 |

## 三、匹配逻辑（多级）

1. **优先匹配**：CBS「交易流水号」↔ SAP「凭证编号」（唯一标识）
2. **辅助匹配**：CBS「交易日期」↔ SAP「过账日期」
3. **金额验证**：CBS「借(支出)/贷(收入)」↔ SAP「公司代码货币价值」+「借/贷标识」
4. **备选匹配**：对未匹配记录再用「日期 + 金额」组合、「账号 + 金额」补充匹配

## 四、输出要求

- 在 CBS 明细中新增列：
  - **SAP 匹配状态**：`已匹配` / `未匹配` / `疑似匹配（需人工确认）`
  - **未匹配原因**（可选）：如「SAP 无此流水号」「金额不一致」等
- 生成**匹配报告**：匹配成功率、未匹配条数、疑似匹配条数等
- 输出文件便于筛选未匹配记录，供人工核对

## 五、技术要点

- 日期格式统一（如统一为 YYYY-MM-DD）
- 金额字段按数值处理，避免文本/千分位导致比较失败
- 支持通过参数或配置指定 CBS 明细、SAP 明细的 Excel 路径（默认可为当前目录或指定目录）

---

## 六、使用方式

1. **激活环境**（在项目目录下）  
   ```bash
   source venv/bin/activate   # macOS/Linux
   ```

2. **准备文件**  
   将 CBS 明细、SAP 明细的 Excel 放在本目录（或使用参数指定路径），默认期望文件名：`CBS明细.xlsx`、`SAP明细.xlsx`。

3. **运行对账**  
   ```bash
   python backend_reconcile_sap_cbs.py
   ```  
   或指定文件：  
   ```bash
   python backend_reconcile_sap_cbs.py --cbs "你的CBS明细.xlsx" --sap "你的SAP明细.xlsx" -o "CBS明细_对账结果.xlsx" --report "对账报告.txt"
   ```

4. **查看结果**  
   - 打开 `CBS明细_对账结果.xlsx`，按列「SAP匹配状态」筛选「未匹配」或「疑似匹配」进行人工核对。  
   - 查看 `对账报告.txt` 了解匹配率与统计。

---

## 七、网页版（供无 Python 用户）

在本机启动 Web 服务后，其他人用浏览器访问即可对账，无需安装 Python。

1. **安装依赖并启动**（在本机项目目录下）：  
   ```bash
   source venv/bin/activate
   pip install -r requirements.txt   # 若尚未安装 flask
   python app.py
   ```
2. **访问地址**  
   - 本机：浏览器打开 `http://127.0.0.1:5000`  
   - 局域网（如公司同一 WiFi）：对方电脑浏览器打开 `http://你的电脑IP:5000`（本机 IP 可用 `ifconfig` 或 `ipconfig` 查看）
3. **使用步骤**：在网页中选择 CBS 明细与 SAP 明细两个 Excel 文件，点击「开始对账」，等待完成后在页面查看报告并点击「下载对账结果.xlsx」「下载对账报告.txt」保存到本地。
