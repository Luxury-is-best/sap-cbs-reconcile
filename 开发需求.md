1. 表头解析结果概览
四个表格的核心信息如下：
表格
表格名称	表头数量	核心关键字段	数据类型
CBS 明细	35 个	交易日期、交易流水号、账号、金额（借 / 贷）、银行流水号、对账码	交易明细
CBS 余额	26 个	账号、账户名称、账户余额、币种、开户行	账户余额
SAP 明细	40 个	凭证编号、过账日期、金额、账户号、公司代码、利润中心	交易明细
SAP 余额	17 个	科目号、余额、货币、期间 / 年度、利润中心	账户余额
2. 关键匹配字段分析（脚本开发核心）
为了实现 “CBS 明细未在 SAP 明细中存在” 的识别功能，需要确定核心匹配字段：
2.1 优先匹配字段（推荐）
交易流水号（CBS） ↔ 凭证编号（SAP）：最可能的唯一标识匹配
交易日期（CBS） ↔ 过账日期（SAP）：时间维度辅助匹配
借 / 贷金额（CBS） ↔ 金额（SAP）：数值维度验证匹配
2.2 备选匹配字段
账号（CBS） ↔ 账户号（SAP）：账户维度匹配
银行流水号（CBS）：可能在 SAP 的备注或描述字段中存在
对账码（CBS）：专门用于对账的编码字段，优先检查
3. 脚本开发建议
基于表头分析，给您的同事（trae）提供以下开发建议：
数据读取阶段
优先读取 CBS 明细和 SAP 明细的完整数据
注意处理日期格式（确保一致性）和金额字段（避免数值格式问题）
匹配逻辑设计
采用多维度匹配：先通过唯一标识（流水号 / 凭证号）精确匹配
对未匹配到的记录，再用 “日期 + 金额” 组合匹配
最后用 “账号 + 金额” 进行补充匹配
结果标注阶段
在 CBS 明细中新增 “SAP 匹配状态” 字段
标注选项：已匹配、未匹配、疑似匹配（需人工确认）
对未匹配记录，可新增 “未匹配原因” 字段（如：SAP 无此流水号、金额不匹配等）
数据验证
统计匹配成功率，生成匹配报告
提供未匹配记录的筛选功能，便于人工核对